USE geo;

SELECT * 
FROM geo.world x, x.mondial.country y,
	 (select value s.name
	 from geo.world x2, x2.mondial.sea as s, split(s.`-country`, " ") as ss
	 where  y.`-car_code` = ss) as w
group by y.name as country_name, y.`-car_code` as country_code group as seas(w as sea)
having array_count(seas)>=2
order by array_count(seas) desc;

-- Duration of all jobs: 1.757 sec